
[stsr]
name = STSR
;simulate = SIM_STSR
simulate = 

; TODO adam5000 address
ip = 0.0.0.0
port = 502

; module address used at start of all commands
aa = 1

; tcp model, if 1 wrap ascii commands in modbus frames
tcp = 1

; NOTE: slot indices are not configured here;
; the control class will query the adam5000
; to determine the slot index for each IO module.
